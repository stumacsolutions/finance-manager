machine:
  services:
    - docker

test:
  override:
    - ./gradlew test
    - ./gradlew acceptanceTest
    - ./gradlew mutationTest

deployment:
  master:
    branch: master
    commands:
      - ./gradlew publishContainer
