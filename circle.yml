machine:
  services:
    - docker

dependencies:
  override:
    - ./gradlew resolveConfigurations

test:
  override:
    - ./gradlew test
    - ./gradlew acceptanceTest
    - ./gradlew mutationTest

deployment:
  master:
    branch: master
    commands:
      - ./gradlew publishContainer
