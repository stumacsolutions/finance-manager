machine:
  services:
    - docker

#dependencies:
#  override:
#    - ./gradlew resolveConfigurations

test:
  override:
    - ./gradlew test mutationTest acceptanceTest

deployment:
  master:
    branch: master
    commands:
      - ./gradlew publishContainer
